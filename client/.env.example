# API base that the admin app and widget call
VITE_API_BASE=http://localhost:4000/api

# Where embed.js is served from (origin only, no trailing slash)
VITE_EMBED_BASE=http://localhost:5173

# Path to the widget index inside the same origin as embed.js
VITE_WIDGET_PATH=/chat-bot/index.html
